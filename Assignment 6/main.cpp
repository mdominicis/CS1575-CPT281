#include <iostream>
#include <vector>
#include <queue>
#include <list>

#include "Solution.h"

using namespace std;

int main() {
    Solution x;

    cout << x.first_missing_letter("datastructures");

   /* List_Node* head = new List_Node(1);
    head->next= new List_Node(2);
    head->next->next = new List_Node(3);
    head->next->next->next = new List_Node(4);
    if(x.has_cycle(head)){
        cout << "has cycle";
    }
    else{
        cout << "no cycle";
    }*/

	/*vector <unsigned int> v = {2,7,4,1,8,1 };
    vector <unsigned int> v2 = { 1,1,1,1 };
    cout<<x.last_weight(v2)<<endl;*/

    /*list<unsigned int> li = x.path(14);
    cout << "[";
	for (const int& value : li) {
        cout << value << ",";
    }
    cout << "]";*/
    return 0;
}